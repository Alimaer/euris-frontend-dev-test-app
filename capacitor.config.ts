import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'euris-frontend-dev-test-app',
  webDir: 'www'
};

export default config;
